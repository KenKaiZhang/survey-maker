import React from "react";
import { Collection } from "../../components/Collection";

export const Category = () => {
  return (
    <React.Fragment>
      <Collection short max={4} />
    </React.Fragment>
  );
};
